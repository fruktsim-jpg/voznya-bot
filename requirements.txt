# Telegram-бот "Возня" — зависимости проекта.
# Версии зафиксированы для воспроизводимого окружения.

# aiogram 3.28.x: добавлены sendGift / getMyStarBalance / getAvailableGifts
# (Bot API 7.10+/9.0+) — реальная авто-выдача Telegram Gifts. ВАЖНО: образ
# собирается из этого файла (Dockerfile: pip install -r requirements.txt),
# поэтому пин обязан совпадать с версией на сервере, иначе пересборка молча
# откатит на 3.13.1 и авто-доставка снова станет недоступной. Точную версию
# сверь на сервере: `pip show aiogram`.
aiogram>=3.28,<3.29

SQLAlchemy[asyncio]==2.0.35
asyncpg==0.29.0
alembic==1.13.2
APScheduler==3.10.4
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# Локальные embeddings для долгой памяти друна (drun/embeddings.py).
# fastembed запускает ONNX-модели через onnxruntime в CPU-режиме внутри
# контейнера бота — не требует GPU, не ходит в сеть, бесплатно. Дефолтная
# модель в ai_settings.embedding_model — sentence-transformers/paraphrase-
# multilingual-MiniLM-L12-v2 (384d, ~120 МБ ONNX, скачивается при первом
# запуске в /root/.cache/fastembed; в docker-compose смонтирован volume
# voznya_fastembed_cache, иначе rebuild = повторное скачивание).
#
# ВАЖНО про пин 0.4.2: в этой версии fastembed выкинул из реестра модель
# intfloat/multilingual-e5-small, которая ДОЛГО была дефолтом в ai_settings
# (см. INCIDENT 2026-06-18: друн неделю молчал из-за ValueError при load
# модели → отравленная asyncpg-транзакция → каскадный сбой generate). Если
# поднимаешь fastembed — сверь, что текущая модель в БД всё ещё в
# TextEmbedding.list_supported_models(), иначе друн снова молчит.
fastembed==0.4.2
